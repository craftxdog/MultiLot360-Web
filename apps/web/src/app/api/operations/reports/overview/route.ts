import { operationsApi } from "@/features/operations/server/operations-api";
import { getOperationsToken, operationsError, operationsResponse } from "@/features/operations/server/operations-route";
import { reportsQuerySchema } from "@/features/operations/utils/operations-query";
import { getCurrentUser } from "@/features/auth/server/get-current-user";
export async function GET(request: Request) { try { const token = await getOperationsToken(); if (!token) return operationsResponse({ message: "Sesión expirada" }, 401); const parsed = reportsQuerySchema.parse(Object.fromEntries(new URL(request.url).searchParams)); const user = await getCurrentUser(); const admin = user?.role.name.toUpperCase() === "ADMIN"; if (!admin && !user?.seller?.id) return operationsResponse({ message: "Tu cuenta no tiene un vendedor asociado." }, 403); const query = { dateFrom: parsed.dateFrom, dateUntil: parsed.dateUntil, sellerId: admin ? parsed.sellerId : user?.seller?.id, drawCode: parsed.drawCode }; return operationsResponse(await operationsApi.report(query, token)); } catch (error) { return operationsError(error); } }
