"use client";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Plus } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { DataPagination } from "@/components/ui/data-pagination";
import { useCurrentUser } from "@/features/auth/hooks/use-current-user";
import { operationKeys, resultsOptions } from "../queries/operations.queries"; import { operationsService } from "../services/operations.service"; import { resultsQuerySchema } from "../utils/operations-query"; import { useUrlQuery } from "../hooks/use-url-query"; import { EmptyState, ErrorState, LoadingRows, OperationsShell } from "./operations-shell";
export function ResultsWorkspace() { const { query, update, pathname } = useUrlQuery(resultsQuerySchema); const result = useQuery(resultsOptions(query)); const user = useCurrentUser(); const client = useQueryClient(); const create = useMutation({ mutationFn: operationsService.createResult, onSuccess: () => { void client.invalidateQueries({ queryKey: operationKeys.results }); toast.success("Resultado registrado"); }, onError: (e) => toast.error(e.message) }); const filters = <form className="grid gap-2 md:grid-cols-3 xl:grid-cols-7" onSubmit={(e) => { e.preventDefault(); const f = new FormData(e.currentTarget); update({ date: String(f.get("date") || "") || undefined, drawCode: String(f.get("drawCode") || "") || undefined, winningNumber: String(f.get("winningNumber") || "") || undefined, shiftId: String(f.get("shiftId") || "") || undefined, createdByUserId: String(f.get("createdByUserId") || "") || undefined, sortBy: String(f.get("sortBy")) as typeof query.sortBy }); }}><Input name="date" type="date" defaultValue={query.date} aria-label="Fecha" /><Input name="drawCode" defaultValue={query.drawCode} placeholder="Sorteo" /><Input name="winningNumber" defaultValue={query.winningNumber} placeholder="Número ganador" inputMode="numeric" maxLength={2} /><Input name="shiftId" defaultValue={query.shiftId} placeholder="ID turno" /><Input name="createdByUserId" defaultValue={query.createdByUserId} placeholder="ID creador" /><select name="sortBy" defaultValue={query.sortBy} className="h-11 rounded-xl border border-border bg-background px-3 text-sm"><option value="createdAt">Más recientes</option><option value="date">Fecha</option><option value="winningNumber">Número</option><option value="drawCode">Sorteo</option></select><Button type="submit" variant="secondary">Aplicar</Button></form>; return <OperationsShell eyebrow="Cierre y ganadores" title="Resultados" description="Registra el número ganador de un turno cerrado y revisa premios pendientes o pagados." actions={user.data?.permissions.includes("resultados.create") ? <details className="relative"><summary className="cursor-pointer list-none"><Button className="h-9 gap-2" tabIndex={-1}><Plus className="h-4 w-4" />Registrar</Button></summary><form className="absolute right-0 z-20 mt-2 w-80 space-y-3 rounded-2xl border border-border bg-background p-4 shadow-2xl" onSubmit={(e) => { e.preventDefault(); const f = new FormData(e.currentTarget); create.mutate({ shiftId: String(f.get("shiftId")), winningNumber: String(f.get("winningNumber")).replace(/\D/g, "").padStart(2,"0") }); }}><Input name="shiftId" placeholder="ID del turno cerrado" required /><Input name="winningNumber" placeholder="Número 00–99" inputMode="numeric" maxLength={2} required /><Button type="submit" className="w-full" disabled={create.isPending}>Confirmar resultado</Button></form></details> : null} filters={filters}>{result.isLoading ? <LoadingRows /> : result.error ? <ErrorState message={result.error.message} /> : !result.data?.data.length ? <EmptyState>No hay resultados con estos filtros.</EmptyState> : <div className="space-y-2">{result.data.data.map((item) => <article key={item.id} className="grid gap-3 rounded-xl border border-border p-4 md:grid-cols-[80px_1fr_repeat(3,auto)] md:items-center"><span className="font-mono text-3xl">{item.winningNumber}</span><div><p className="text-sm font-medium">{item.shift.configuration.code}</p><p className="text-xs text-muted-foreground">{item.shift.date} · {item.shift.status}</p></div><span className="text-xs">{item.winnerSummary.winningSalesCount} ganadores</span><span className="text-xs text-amber-500">{item.winnerSummary.pendingSalesCount} pendientes</span><span className="font-mono text-xs">{item.winnerSummary.totalPrizeMiles.toFixed(2)} mil</span></article>)}</div>}{result.data ? <DataPagination basePath={pathname} params={query} pagination={result.data.pagination} itemLabel="resultados" /> : null}</OperationsShell>; }
