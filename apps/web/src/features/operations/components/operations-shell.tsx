import type { ReactNode } from "react";
import { RadioTower } from "lucide-react";
export function OperationsShell({ eyebrow, title, description, actions, filters, children }: { eyebrow: string; title: string; description: string; actions?: ReactNode; filters: ReactNode; children: ReactNode }) { return <div className="mx-auto max-w-7xl space-y-6"><header className="flex flex-col gap-4 border-b border-border pb-6 md:flex-row md:items-end md:justify-between"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-1 text-[11px] uppercase tracking-[0.18em] text-muted-foreground"><RadioTower className="h-3.5 w-3.5" />{eyebrow}</div><h1 className="text-2xl font-medium tracking-[-0.04em]">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">{description}</p></div>{actions}</header><section className="rounded-2xl border border-border bg-card p-4">{filters}<div className="mt-5">{children}</div></section></div>; }
export function EmptyState({ children }: { children: ReactNode }) { return <div className="rounded-xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground">{children}</div>; }
export function LoadingRows() { return <div className="space-y-2">{[0,1,2,3].map((item) => <div key={item} className="h-20 animate-pulse rounded-xl bg-muted" />)}</div>; }
export function ErrorState({ message }: { message: string }) { return <div role="alert" className="rounded-xl border border-danger/25 bg-danger/10 p-4 text-sm text-danger">{message}</div>; }
